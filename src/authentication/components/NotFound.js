import React, { Fragment } from 'react';

const SignUp = () => (
  <Fragment>
    <h1>Oops!</h1>
    <h2>The page you were looking for doesn't seem to exist</h2>
  </Fragment>
);

export default SignUp;
